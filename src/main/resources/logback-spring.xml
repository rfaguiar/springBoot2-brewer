<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <!-- trace_id/span_id injetados no MDC pelo OpenTelemetry Java Agent (correlação com o SigNoz) -->
    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd'T'HH:mm:ss.SSSXXX}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %clr([trace_id=%X{trace_id:-},span_id=%X{span_id:-}]){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}" />

    <include resource="org/springframework/boot/logging/logback/base.xml" />

    <logger name="com.brewer" level="debug" />

    <springProfile name="default">
        <logger name="org.hibernate.SQL" level="debug" />
    </springProfile>
</configuration>